// Settings (SPEC §8.1: accounts, categories, payees/rules, tags, rates,
// imports, backup, appearance, storage). Placeholder — replaced by the
// Settings build agent.
export default function Settings() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Settings</h1>
      <p className="mt-2 text-sm text-muted">Being built.</p>
    </div>
  );
}
