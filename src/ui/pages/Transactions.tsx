// Transaction register (SPEC §8.1.2). Placeholder — replaced by the
// Transactions build agent.
export default function Transactions() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Transactions</h1>
      <p className="mt-2 text-sm text-muted">Being built.</p>
    </div>
  );
}
